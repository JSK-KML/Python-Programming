import{_ as l}from"./chunks/lab-2-1.BIoSFs8y.js";import{_ as c}from"./chunks/c2t1-1.DJdG3pDF.js";import{_ as d,C as s,o as p,c as h,a3 as a,G as i,w as u,j as o,a as t}from"./chunks/framework.DBrjlGeB.js";const g="/Python-Programming/assets/lab-3-1.Bzbi0RAZ.png",m="/Python-Programming/assets/lab-3-2.CIbIf7w8.png",b="/Python-Programming/assets/lab-3-3.m5zoibre.png",y="/Python-Programming/assets/lab-3-5.C9Y4vtP-.png",f="/Python-Programming/assets/lab-3-4.xBkNl6ie.png",w="/Python-Programming/assets/lab-3-6.BGoPIQ_-.png",k="/Python-Programming/assets/lab-3-7.-6OFbnB8.png",x="/Python-Programming/assets/lab-3-8.BdoFm88f.png",M=JSON.parse('{"title":"Lab 3 - More Flowgorithm","description":"","frontmatter":{"outline":"deep","title":"Lab 3 - More Flowgorithm"},"headers":[],"relativePath":"labs/lab-03.md","filePath":"labs/lab-03.md"}'),v={name:"labs/lab-03.md"},C={id:"recap-building-the-sequence",tabindex:"-1"},P={id:"exercise-2-–-hotel-reservation",tabindex:"-1"},T={id:"exercise-3-–-storage-conversion",tabindex:"-1"},_={id:"exercise-4-–-international-fax-cost",tabindex:"-1"},A={id:"exercise-5-–-cookie-calories",tabindex:"-1"},F={id:"exercise-6-–-wage-to-mow-yard",tabindex:"-1"},I={id:"exercise-7-–-temperature-conversion",tabindex:"-1"},S={id:"exercise-8-–-simple-interest-calculator",tabindex:"-1"};function E(B,e,q,R,V,O){const r=s("Checklist"),n=s("Badge");return p(),h("div",null,[e[25]||(e[25]=a('<h1 id="lab-03-more-flowgorithm" tabindex="-1">Lab 03 : More Flowgorithm <a class="header-anchor" href="#lab-03-more-flowgorithm" aria-label="Permalink to &quot;Lab 03 : More Flowgorithm&quot;">​</a></h1><h2 id="before-you-begin" tabindex="-1">Before You Begin <a class="header-anchor" href="#before-you-begin" aria-label="Permalink to &quot;Before You Begin&quot;">​</a></h2><p>Same as every lab, before you start, make sure your workspace and login are <strong>yours</strong>:</p><ol><li>Open <strong>your own</strong> cloned folder in <strong>VS Code</strong> (<code>File</code> → <code>Open Folder...</code>). Many students share the same PC, so confirm the folder belongs to you.</li><li>In the terminal, log out of any account with <code>gh auth logout</code>, then log back in as yourself with <code>gh auth login</code>.</li></ol><p>Full steps are in <a href="./lab-02.html#before-you-begin">Lab 02 - Before You Begin</a> if you need a reminder.</p><h2 id="pull-and-update-in-vs-code" tabindex="-1">Pull and Update in VS Code <a class="header-anchor" href="#pull-and-update-in-vs-code" aria-label="Permalink to &quot;Pull and Update in VS Code&quot;">​</a></h2><p>This lab builds on <a href="./lab-02.html">Lab 02</a>. There you learned the <strong>sequence</strong> structure: taking input, declaring variables, and showing output. Here you will add the missing piece, <strong>processing</strong> that input with calculations, then run your flowchart and export it to <strong>Python</strong>.</p><p>Before starting any lab, you need to make sure that the repo in your <strong>GitHub</strong> is the latest one. <a href="./lab-01.html#syncing-fork">Sync the repo</a> if the <code>upstream</code> repo have been updated.</p><p>Once the online repo is in-sync, bring those changes down to your PC by clicking <code>Source Control</code> and then <code>...</code> beside <code>Changes</code> and click <a href="./glossary.html#pull"><code>Pull</code></a>.</p><p align="center"><img src="'+l+'" alt="drawing" width="400"></p><h2 id="checklist-before-you-proceed" tabindex="-1">Checklist Before You Proceed <a class="header-anchor" href="#checklist-before-you-proceed" aria-label="Permalink to &quot;Checklist Before You Proceed&quot;">​</a></h2>',11)),i(r,{title:"Please Check Properly"},{default:u(()=>[...e[0]||(e[0]=[o("ol",null,[o("li",null,"You have logged in to your Github account in the browser."),o("li",null,"You have logged in to your Github account in the terminal using VS Code."),o("li",null,"You have sync the repo in you Github account in the browser."),o("li",null,"You have pull the update into you repo in VS Code.")],-1)])]),_:1}),o("h2",C,[e[1]||(e[1]=t("Recap: Building the Sequence ",-1)),i(n,{type:"warning",text:"Recall"}),e[2]||(e[2]=t()),e[3]||(e[3]=o("a",{class:"header-anchor",href:"#recap-building-the-sequence","aria-label":'Permalink to "Recap: Building the Sequence <Badge type="warning" text="Recall" />"'},"​",-1))]),e[26]||(e[26]=a('<p>We will reuse the meal-price scenario from <a href="./../tutorials/chapter-2-tutorial-1.html#scenario-1-meal-price-with-taxes">Chapter 2 - Tutorial 1, Scenario 1</a>:</p><blockquote><p>A government tax of 10% and service tax of 5% will be added to the price of a set of meal bought at MFC. Calculate the total price that Ali has to pay for a set of meal.</p></blockquote><p>You already know how to do every step below from Lab 02, so we will not repeat the screenshots. If any step is unclear, go back to <a href="./lab-02.html#creating-our-first-flowchart">Lab 02 - Creating Our First Flowchart</a>.</p><p>Launch <strong>Flowgorithm</strong>, then <code>File</code> → <code>Save As...</code> and name the file <code>lab-03-1</code>. Build the following sequence by clicking the arrow where each shape should go:</p><ol><li><strong>Declare</strong> <code>mealPrice</code> (Integer)</li><li><strong>Input</strong> <code>mealPrice</code></li><li><strong>Declare</strong> <code>finalPrice</code> (Real)</li><li><strong>Output</strong> <code>finalPrice</code></li></ol><p>When you finish, you should have these four shapes in order, with an empty gap between <code>Declare finalPrice</code> and <code>Output finalPrice</code> where the calculation will go.</p><div class="warning custom-block"><p class="custom-block-title">REMINDER</p><p>Before moving on, confirm you have <strong>four</strong> shapes in order: declare and input for <code>mealPrice</code>, then declare and output for <code>finalPrice</code>. The calculation is still missing, that is what we add next.</p></div><h2 id="creating-a-process-the-assign-shape" tabindex="-1">Creating a Process (The Assign Shape) <a class="header-anchor" href="#creating-a-process-the-assign-shape" aria-label="Permalink to &quot;Creating a Process (The Assign Shape)&quot;">​</a></h2><p>So far every flowchart you have built only moves data in and straight back out. A real program has to <strong>do something</strong> to that data in between. That &quot;doing something&quot; is a <strong>process</strong>, and in <strong>Flowgorithm</strong> a process is created with the <code>Assign</code> shape.</p><p>The <code>Assign</code> shape takes an <strong>expression</strong> on the right, calculates its value, and stores the result in a <strong>variable</strong> on the left. This is where the actual maths of your program happens.</p><p>To add it, click the arrow between <code>Declare finalPrice</code> and <code>Output finalPrice</code>.</p><p align="center"><img src="'+g+'" alt="drawing" width="200"></p><p>In the popup window, under the category <strong>Statement</strong>, choose the <code>Assign</code> shape.</p><p align="center"><img src="'+m+'" alt="drawing" width="500"></p><p>Double click the new <code>Assign</code> shape. The popup has two boxes: the <strong>Variable</strong> box on the left (where the answer is stored) and the <strong>Expression</strong> box on the right (the calculation). For our scenario, the total is the meal price plus 10% government tax plus 5% service tax, so:</p><ul><li><strong>Variable:</strong> <code>finalPrice</code></li><li><strong>Expression:</strong> <code>mealPrice + (mealPrice * 0.10) + (mealPrice * 0.05)</code></li></ul><p align="center"><img src="'+b+'" alt="drawing" width="400"></p><div class="info custom-block"><p class="custom-block-title">NOTE</p><p>You can only assign <strong>into</strong> a variable that has already been declared, and you can only use variables in the expression that already hold a value. This is why we declared <code>finalPrice</code> earlier and put the <code>Assign</code> <strong>after</strong> the input, not before it. The order matters.</p></div><h3 id="adding-more-than-one-input-or-output" tabindex="-1">Adding More Than One Input or Output <a class="header-anchor" href="#adding-more-than-one-input-or-output" aria-label="Permalink to &quot;Adding More Than One Input or Output&quot;">​</a></h3><p>Our meal-price example used a single input and a single output, but several of the exercises below need more than one of each (the storage converter, for example, has one input and <strong>five</strong> outputs).</p><p>Adding them works exactly the same way: click the arrow at the point in the sequence where the new shape should go, then add another <code>Declare</code>, <code>Input</code>, <code>Assign</code>, or <code>Output</code>. Every variable must still be declared before it is used.</p><div class="warning custom-block"><p class="custom-block-title">REMINDER</p><p>The auto-checker reads your inputs and outputs <strong>in the order they appear from top to bottom</strong>. Place each <code>Input</code> and <code>Output</code> in the exact order the question lists, or your check will fail even when the maths is correct.</p></div><h2 id="running-and-testing" tabindex="-1">Running and Testing <a class="header-anchor" href="#running-and-testing" aria-label="Permalink to &quot;Running and Testing&quot;">​</a></h2><p>A flowchart that looks correct is not the same as a flowchart that <strong>is</strong> correct. <strong>Flowgorithm</strong> lets you <a href="./glossary.html#run">run</a> your flowchart step by step so you can check the logic against your own calculation.</p><p>Before running, double-check your flowchart matches this exactly:</p><p align="center"><img src="'+y+'" alt="drawing" width="200"></p><p>To run the program, click the <strong>green arrow</strong> at the top right corner of the window. A popup will appear asking for input. Enter a value for <code>mealPrice</code>, for example <code>100</code>.</p><p align="center"><img src="'+f+'" alt="drawing" width="400"></p><p>Now <strong>verify</strong> the result yourself. For an input of <code>100</code>, the total should be <code>100 + 10 + 5 = 115</code>. If <strong>Flowgorithm</strong> shows <code>115</code>, your process is correct. If it shows something else, double click your <code>Assign</code> shape and check the expression.</p><div class="tip custom-block"><p class="custom-block-title">WHY</p><p>Always test with a value you can work out by hand. If your flowchart and your calculator disagree, the bug is in the flowchart, and now you know to fix it before you ever write a line of code.</p></div><p>Once you are happy with the result, create a folder for this work: in <strong>VS Code</strong>, right click the <code>lab03</code> folder, choose <code>New Folder</code>, and name it <code>exercise1</code>. Copy the saved <code>lab-03-1.fprg</code> file into <code>/labs/lab03/exercise1/</code>, then <a href="./lab-01.html#commit-and-push">commit and push</a>.</p><h2 id="exporting-flowgorithm-to-python" tabindex="-1">Exporting Flowgorithm to Python <a class="header-anchor" href="#exporting-flowgorithm-to-python" aria-label="Permalink to &quot;Exporting Flowgorithm to Python&quot;">​</a></h2><p>One of the most useful features of <strong>Flowgorithm</strong> is that it can translate your finished flowchart into real code in many languages, including <strong>Python</strong>. This lets you connect the diagram you understand to the code you are about to learn.</p><p>First, in <strong>VS Code</strong>, right click the <code>exercise1</code> folder you just created and click <code>New File</code>. Name the file <code>lab-03-1.py</code>.</p><p>Back in <strong>Flowgorithm</strong>, at the top bar, click the <code>Source Code Viewer</code>.</p><p align="center"><img src="'+w+'" alt="drawing" width="400"></p><p>A popup window will appear. Click the dropdown at the top left and choose <code>Python</code>.</p><p align="center"><img src="'+k+'" alt="drawing" width="400"></p><p>Copy the generated code from the window into your <code>lab-03-1.py</code> file and save it.</p><div class="info custom-block"><p class="custom-block-title">NOTE</p><p>Take a moment to read the <strong>Python</strong> code it produced. Each <code>Declare</code>, <code>Input</code>, <code>Assign</code>, and <code>Output</code> shape in your flowchart became a line of code. You will be writing this kind of code by hand soon, so seeing the flowchart and the code side by side is good practice.</p></div><h2 id="working-with-the-command-line" tabindex="-1">Working with the Command Line <a class="header-anchor" href="#working-with-the-command-line" aria-label="Permalink to &quot;Working with the Command Line&quot;">​</a></h2><p>So far you have run your flowchart with the green arrow and managed your files by clicking. Programmers do much of this same work by typing into the <a href="./glossary.html#terminal">terminal</a> instead, giving the computer one <a href="./glossary.html#command">command</a> at a time. In this section you will run the <strong>Python</strong> file you just exported entirely from the command line, picking up four everyday commands as you go.</p><p>Open a terminal in <strong>VS Code</strong> with <code>Terminal</code> → <code>New Terminal</code>. A panel appears at the bottom, with a line waiting for your first command.</p><p>A terminal is always &quot;inside&quot; one folder, called the <a href="./glossary.html#working-directory">working directory</a>, so the first thing to do is ask where you are:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">pwd</span></span></code></pre></div><p><a href="./glossary.html#pwd"><code>pwd</code></a> (<em>print working directory</em>) prints the full path of the folder you are currently in. The terminal responds with <strong>output</strong> like this:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/c/Users/Aiman/Desktop/name-cp115/CP115-Class-Repo</span></span></code></pre></div><p><code>pwd</code> depends on where do you open your VS Code, for some of you, the output of <code>pwd</code> might be:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/c/Users/Aiman/Desktop/name-cp115</span></span></code></pre></div><p>Dont worry too much about it, because we can change our <code>pwd</code> later.</p><p>Now lets take a look at what you have in the current directory. Run the command below:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ls</span></span></code></pre></div><p><a href="./glossary.html#ls"><code>ls</code></a> (<em>list</em>) shows the files and folders inside your working directory. This time the output is:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>labs  README.md</span></span></code></pre></div><p>You might notice the <code>Explorer</code> panel of your <code>CP115-Class-Repo</code> shows more than this, including <code>.github</code> and <code>.vscode</code>:</p><p align="center"><img src="'+x+'" alt="drawing" width="300"></p><p>Those two folders start with a dot (<code>.</code>), and <code>ls</code> hides any file or folder whose name begins with a dot. <strong>VS Code</strong> still shows them in the <code>Explorer</code>; they are there, <code>ls</code> simply does not list them unless you ask it to. You can ignore them for this lab.</p><p>Now lets change our <code>pwd</code>, to do that we use <code>cd</code> ( <em>change directory</em> ). You can do them one by one like below:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> labs</span></span></code></pre></div><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lab03</span></span></code></pre></div><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exercise1</span></span></code></pre></div><p>Now if you try to run <code>pwd</code> again, you will see the output below:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/c/Users/Aiman/Desktop/name-cp115/CP115-Class-Repo/labs/lab03/exercise1</span></span></code></pre></div><p>Run <code>ls</code> here, and the output lists the two files you saved earlier:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>lab-03-1.fprg  lab-03-1.py</span></span></code></pre></div><p>That confirms you are in the right place. Now run the file with the <strong>Python</strong> interpreter:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lab-03-1.py</span></span></code></pre></div><p>The program runs, asks for your input just as before, and prints the result straight into the terminal.</p><div class="info custom-block"><p class="custom-block-title">NOTE</p><p>If <code>python</code> is not recognised, try <code>py lab-03-1.py</code> instead. On <strong>Windows</strong> the Python launcher is sometimes called <code>py</code>.</p></div><div class="tip custom-block"><p class="custom-block-title">WHY</p><p>Clicking the ▶ button does all of this for you: it opens a terminal and runs <code>python</code> on your file behind the scenes. Lab 01 made the same point about the <code>Source Control</code> panel running <code>git clone</code>. Learning the commands underneath means you understand what the button is really doing.</p></div><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><p>The remaining tasks are yours to solve. For each one, build the flowchart in <strong>Flowgorithm</strong>, test it, export it to <strong>Python</strong>, and place both files in that exercise&#39;s folder.</p><div class="warning custom-block"><p class="custom-block-title">REMINDER</p><p>Your program must output <strong>only the answer</strong>, nothing else. In each <code>Output</code> shape put <strong>just the variable</strong> (for example <code>finalPrice</code>), never a label such as <code>&quot;Total: &quot; &amp; finalPrice</code>. Do not add any prompt text to your <code>Input</code> either. The auto-checker reads the raw numbers your program prints, in the given order, so any extra words, labels, or symbols will fail the check even when your calculation is correct.</p></div>',73)),o("h2",P,[e[4]||(e[4]=t("Exercise 2 – Hotel Reservation ",-1)),i(n,{type:"warning",text:"Question"}),e[5]||(e[5]=t()),e[6]||(e[6]=o("a",{class:"header-anchor",href:"#exercise-2-–-hotel-reservation","aria-label":'Permalink to "Exercise 2 – Hotel Reservation <Badge type="warning" text="Question" />"'},"​",-1))]),e[27]||(e[27]=a("<p>Question: Mr Rahman plans to make a reservation for a hotel room in Langkawi Island. The basic rate is RM250.00 per night and service charge is 15% of the room rate. Determine the total payment if he books the hotel room for n nights.</p><p>Instruction:</p><ul><li>Flowgorithm file name: <code>lab-03-2</code> → place in <code>/labs/lab03/exercise2/</code>.</li><li>Python file name : <code>lab-03-2.py</code> → place in <code>/labs/lab03/exercise2/</code>.</li></ul><p>Inputs (in order):</p><ul><li><code>numNight</code> – integer number of nights</li></ul><p>Outputs (in order):</p><ul><li><code>totalPayment</code> – sum of room rate and service charge (RM)</li></ul><p>After exporting, <strong>run <code>lab-03-2.py</code> in the terminal</strong> and test it yourself by entering an input to confirm it works.</p>",8)),o("h2",T,[e[7]||(e[7]=t("Exercise 3 – Storage Conversion ",-1)),i(n,{type:"warning",text:"Question"}),e[8]||(e[8]=t()),e[9]||(e[9]=o("a",{class:"header-anchor",href:"#exercise-3-–-storage-conversion","aria-label":'Permalink to "Exercise 3 – Storage Conversion <Badge type="warning" text="Question" />"'},"​",-1))]),e[28]||(e[28]=a("<p>Question: Prompt user to input a size in Kilobyte, and convert the input into Megabyte, Gigabyte, Terabyte and Petabyte.</p><p>Instruction:</p><ul><li>Flowgorithm file name: <code>lab-03-3</code> → place in <code>/labs/lab03/exercise3/</code>.</li><li>Python file name : <code>lab-03-3.py</code> → place in <code>/labs/lab03/exercise3/</code>.</li></ul><p>Inputs (in order):</p><ul><li><code>kilobyte</code> – real number in KB</li></ul><p>Outputs (in order):</p><ul><li><p><code>megabyte</code></p></li><li><p><code>gigabyte</code></p></li><li><p><code>terabyte</code></p></li><li><p><code>petabyte</code></p></li></ul><p>After exporting, <strong>run <code>lab-03-3.py</code> in the terminal</strong> and test it yourself by entering an input to confirm it works.</p>",8)),o("h2",_,[e[10]||(e[10]=t("Exercise 4 – International Fax Cost ",-1)),i(n,{type:"warning",text:"Question"}),e[11]||(e[11]=t()),e[12]||(e[12]=o("a",{class:"header-anchor",href:"#exercise-4-–-international-fax-cost","aria-label":'Permalink to "Exercise 4 – International Fax Cost <Badge type="warning" text="Question" />"'},"​",-1))]),e[29]||(e[29]=a("<p>Question: Amin’s program calculates the cost of sending an international fax: service charge RM3.00 plus RM0.20 per page.</p><p>Instruction:</p><ul><li>Flowgorithm file name: <code>lab-03-4</code> → place in <code>/labs/lab03/exercise4/</code>.</li><li>Python file name : <code>lab-03-4.py</code> → place in <code>/labs/lab03/exercise4/</code>.</li></ul><p>Inputs (in order):</p><ul><li><code>pages</code> – integer number of pages</li></ul><p>Outputs (in order):</p><ul><li><code>totalCost</code> – sum of service charge and pages cost</li></ul><p>After exporting, <strong>run <code>lab-03-4.py</code> in the terminal</strong> and test it yourself by entering an input to confirm it works.</p>",8)),o("h2",A,[e[13]||(e[13]=t("Exercise 5 – Cookie Calories ",-1)),i(n,{type:"warning",text:"Question"}),e[14]||(e[14]=t()),e[15]||(e[15]=o("a",{class:"header-anchor",href:"#exercise-5-–-cookie-calories","aria-label":'Permalink to "Exercise 5 – Cookie Calories <Badge type="warning" text="Question" />"'},"​",-1))]),e[30]||(e[30]=a("<p>Question: A pack contains 40 pieces of cookies. The calorie info claims 10 servings per pack. Each serving equals 300 calories. Calculate the total calories consumed based on number of packs eaten.</p><p>Instruction:</p><ul><li>Flowgorithm file name: <code>lab-03-5</code> → place in <code>/labs/lab03/exercise5/</code>.</li><li>Python file name : <code>lab-03-5.py</code> → place in <code>/labs/lab03/exercise5/</code>.</li></ul><p>Inputs (in order):</p><ul><li><code>packsEaten</code> – integer number of packs consumed</li></ul><p>Outputs (in order):</p><ul><li><code>totalCalories</code> – total calories consumed</li></ul><p>After exporting, <strong>run <code>lab-03-5.py</code> in the terminal</strong> and test it yourself by entering an input to confirm it works.</p>",8)),o("h2",F,[e[16]||(e[16]=t("Exercise 6 – Wage to Mow Yard ",-1)),i(n,{type:"warning",text:"Question"}),e[17]||(e[17]=t()),e[18]||(e[18]=o("a",{class:"header-anchor",href:"#exercise-6-–-wage-to-mow-yard","aria-label":'Permalink to "Exercise 6 – Wage to Mow Yard <Badge type="warning" text="Question" />"'},"​",-1))]),e[31]||(e[31]=a('<p>Question: The wage to mow the yard is based on diesel price RM2.00 per square meter. The yard area is the large rectangle minus the house rectangle. You are given the length and width of the yard boundary and the length and width of the house. Calculate the wage.</p><p><img src="'+c+'" alt="image1"></p><p>Instruction:</p><ul><li>Flowgorithm file name: <code>lab-03-6</code> → place in <code>/labs/lab03/exercise6/</code>.</li><li>Python file name : <code>lab-03-6.py</code> → place in <code>/labs/lab03/exercise6/</code>.</li></ul><p>Inputs (in order):</p><ul><li><p><code>yardLength</code> – real number</p></li><li><p><code>yardWidth</code> – real number</p></li><li><p><code>houseLength</code> – real number</p></li><li><p><code>houseWidth</code> – real number</p></li></ul><p>Outputs (in order):</p><ul><li><code>wage</code> – Total wage</li></ul><p>After exporting, <strong>run <code>lab-03-6.py</code> in the terminal</strong> and test it yourself by entering the inputs to confirm it works.</p>',9)),o("h2",I,[e[19]||(e[19]=t("Exercise 7 – Temperature Conversion ",-1)),i(n,{type:"warning",text:"Question"}),e[20]||(e[20]=t()),e[21]||(e[21]=o("a",{class:"header-anchor",href:"#exercise-7-–-temperature-conversion","aria-label":'Permalink to "Exercise 7 – Temperature Conversion <Badge type="warning" text="Question" />"'},"​",-1))]),e[32]||(e[32]=a("<p>Question Prompt user to input a temperature in Celsius and convert it to Fahrenheit and Kelvin.</p><p>Instruction:</p><ul><li>Flowgorithm file name: <code>lab-03-7</code> → place in <code>/labs/lab03/exercise7/</code>.</li><li>Python file name: <code>lab-03-7.py</code> → place in <code>/labs/lab03/exercise7/</code>.</li></ul><p>Inputs (in order):</p><ul><li><code>celsius</code> – real number (°C)</li></ul><p>Outputs (in order):</p><ul><li><p><code>fahrenheit</code></p></li><li><p><code>kelvin</code></p></li></ul><p>After exporting, <strong>run <code>lab-03-7.py</code> in the terminal</strong> and test it yourself by entering an input to confirm it works.</p>",8)),o("h2",S,[e[22]||(e[22]=t("Exercise 8 – Simple Interest Calculator ",-1)),i(n,{type:"warning",text:"Question"}),e[23]||(e[23]=t()),e[24]||(e[24]=o("a",{class:"header-anchor",href:"#exercise-8-–-simple-interest-calculator","aria-label":'Permalink to "Exercise 8 – Simple Interest Calculator <Badge type="warning" text="Question" />"'},"​",-1))]),e[33]||(e[33]=a('<p>Question: Prompt user for principal (P), annual rate (R %) and time (T years); calculate:</p><ol><li>the simple interest earned over the entire period</li><li>the total amount to be repaid</li><li>the monthly interest amount (simple interest ÷ (T × 12))</li></ol><p>Instruction:</p><ul><li>Flowgorithm file name: <code>lab-03-8</code> → place in <code>/labs/lab03/exercise8/</code>.</li><li>Python file name: <code>lab-03-8.py</code> → place in <code>/labs/lab03/exercise8/</code>.</li></ul><p>Inputs (in order):</p><ul><li><code>principal</code> – real number (RM)</li><li><code>rate</code> – real number (% per annum)</li><li><code>time</code> – real number (years)</li></ul><p>Outputs (in order):</p><ul><li><code>interest</code></li><li><code>totalAmount</code></li><li><code>monthlyInterest</code></li></ul><p>After exporting, <strong>run <code>lab-03-8.py</code> in the terminal</strong> and test it yourself by entering the inputs to confirm it works.</p><h2 id="commit-push-and-check" tabindex="-1">Commit, Push and Check <a class="header-anchor" href="#commit-push-and-check" aria-label="Permalink to &quot;Commit, Push and Check&quot;">​</a></h2><p>For <strong>every</strong> exercise you must place <strong>two</strong> files inside that exercise&#39;s own folder under <code>/labs/lab03/</code>:</p><ul><li>the <strong>Flowgorithm</strong> file (<code>.fprg</code>), and</li><li>the exported <strong>Python</strong> file (<code>.py</code>),</li></ul><p>both named exactly as the question states and placed in the matching folder, for example <code>lab-03-2.fprg</code> and <code>lab-03-2.py</code> inside <code>/labs/lab03/exercise2/</code>. A wrong name or wrong folder will fail the check.</p><p>Once all your files are in their exercise folders, submit them in three steps:</p><ol><li><strong>Commit.</strong> In <strong>VS Code</strong>, open <code>Source Control</code>, write a clear message such as <code>Complete Lab 03 exercises</code>, and click <code>Commit</code>. This records a snapshot of your work.</li><li><strong>Push.</strong> Click <code>Sync Changes</code> to upload your commit to your repository on <strong>GitHub</strong>. If you have forgotten how, refer back to <a href="./lab-01.html#commit-and-push">commit and push</a>.</li><li><strong>Check.</strong> The push triggers the <strong>GitHub Actions</strong> auto-checker automatically. Go to your repository on <strong>GitHub</strong> and look at the mark beside your latest commit: an orange dot means it is still running, a red cross means it failed, and a green tick means every file passed. Recall the details <a href="./lab-02.html#using-flowgorithm-with-github-action">here</a>.</li></ol><p>If you see a red cross, click the <code>Actions</code> tab and open the latest workflow to read which file failed and why, then fix it and push again.</p><div class="tip custom-block"><p class="custom-block-title">WHY</p><p>For each Python file the checker <strong>runs your program</strong>, types the inputs in the exact order the question lists, then compares your printed outputs against the expected answers, again in order. This is why every exercise states its inputs and outputs <em>in order</em>. If your program reads inputs in a different order, or prints outputs in a different order, the check fails even when your maths is correct.</p></div>',17))])}const Q=d(v,[["render",E]]);export{M as __pageData,Q as default};
